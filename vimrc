set clipboard=unnamed "use the system clipboard instead of vim
set number "have line numbers
set t_Co=256 "256 colour mode
syntax enable
set ignorecase "ignore case when searching through file
set tabstop=4 "tab spacing
set expandtab "use spaces instead of tabs
